interface:
  display_name: "ResourceToggle"
  short_description: "Repo-specific patterns and workflows for ResourceToggle"
  default_prompt: "Use the ResourceToggle repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true